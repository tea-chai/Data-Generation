

The script Household_Generation.py generates a dataset for energy consumption of 300 households.

The script PV_generation.py produces a dataset for energy generated by PV panels.

The Python script sum.py retrieves all the data profiles available at https://www.loadprofilegenerator.de/results2/ and transforms the data, which is originally recorded in minutes, into hourly data.

